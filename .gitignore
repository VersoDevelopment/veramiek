# Zware mediabestanden horen niet in git (los op de server plaatsen)
*.mp4
*.mov

# Oude/experimentele pagina (wordt ook door nginx geblokkeerd)
index-v2.html

# Tailwind build-dependencies (config + input.css worden wel getrackt)
.build/node_modules/
.backups/

# Cache van de impeccable-hook
.impeccable/

# Wachtwoordbestand voor de afgeschermde proefversie (staat op de server)
.htpasswd
